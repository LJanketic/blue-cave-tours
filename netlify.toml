# https://docs.astro.build/en/guides/deploy/netlify/
[build]
	command = "pnpm run build"
	publish = "dist"

[build.environment]
	NODE_VERSION = "22"

# Set PUBLIC_SITE_URL in the Netlify UI for production canonical URLs.
